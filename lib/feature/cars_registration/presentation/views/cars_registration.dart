import 'package:flutter/material.dart';

class CarsRegister extends StatelessWidget {
  const CarsRegister({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
